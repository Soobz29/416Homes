# Project Status

Read the task table in CLAUDE.md and report which tasks are done `[x]` and
which are pending `[ ]`. Then immediately begin the first pending task by
reading its command file and executing every step in it.

Do not wait for confirmation. Start working.
