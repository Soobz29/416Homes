# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1

# Database
supabase>=2.28,<3

# Telegram
python-telegram-bot==22.6

# AI APIs (lazy-loaded — not imported at startup, only when jobs are processed)
google-genai>=1.0.0,<2
elevenlabs==1.54.0
google-cloud-texttospeech>=2.16.0
gtts>=2.5.0
# google-cloud-aiplatform excluded: 300MB+ of grpc/protobuf dependencies bloat
# startup RAM. Veo renderer lazy-imports it — add back if Veo is needed.

# Payments + notifications
stripe==9.4.0
resend==2.0.0

# HTTP / scraping helpers
requests==2.31.0
httpx>=0.28.1,<0.29
beautifulsoup4==4.12.3
browserforge>=0.11.0

# Scheduling
APScheduler==3.11.2

# Auth
python-jose[cryptography]==3.3.0

# Utils
python-multipart==0.0.9
aiohttp==3.9.3
Pillow>=10.0.0
